{
  "name": "@crediblemark/wagent",
  "version": "0.2.5",
  "description": "WAGENT - WhatsApp AI Agent Platform",
  "type": "module",
  "main": "packages/cli/dist/index.js",
  "bin": {
    "wagent": "packages/cli/dist/index.js"
  },
  "files": [
    "packages/*/dist/**",
    "prompts/**",
    "schemas/**",
    "bin/wagent",
    "bin/wagent.service",
    "LICENSE",
    "README.md"
  ],
  "scripts": {
    "build": "cd packages/core && bun run build && cd ../dashboard && bun run build && cd ../tui && bun run build && cd ../whatsapp && bun run build && cd ../cli && bun run build",
    "prepublishOnly": "npm run build",
    "postinstall": "node packages/whatsapp/scripts/patch-baileys.mjs",
    "start": "./bin/wagent start",
    "dev": "./bin/wagent start --no-dashboard",
    "test": "cd packages/core && npx vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf packages/*/dist node_modules",
    "prepare": "husky"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@crediblemark/core": "*",
    "@crediblemark/cli": "*",
    "@crediblemark/whatsapp": "*",
    "@crediblemark/tui": "*",
    "@crediblemark/dashboard": "*"
  },
  "keywords": [
    "whatsapp",
    "ai",
    "chatbot",
    "customer-service",
    "baileys",
    "agent",
    "automation"
  ],
  "author": "crediblemark",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/crediblemark-official/WAGENT.git"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "husky": "^9.1.7"
  }
}
